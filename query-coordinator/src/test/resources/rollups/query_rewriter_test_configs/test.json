{
  "schemas": {
    "_": {
      "aaaa-aaa1": [
        "text",
        "A"
      ],
      "aaaa-aaa2": [
        "number",
        "B"
      ]
    }
  },
  "rollups": {
    "r1": "SELECT sum(`B`) / count(`B`) GROUP BY `A`"
  },
  "tests": [
    {
      "query": "SELECT sum(`B`) / count(`B`) GROUP BY `A`",
      "rewrites": {
        "r1": "SELECT sum(`B`) / count(`B`) GROUP BY `A`"
      }
    }
  ]
}
